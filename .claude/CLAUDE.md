# Claude / project context

For Go work in this repo, prefer the Context7 standard-library overview:

https://context7.com/golang/go/llms.txt?tokens=10000

See also the repository root `AGENTS.md` for the same link and quick test command (`go test ./...` from `backend/`).
